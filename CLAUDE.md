# ColorManRun

Unity 2D 프로젝트.

## 커밋 컨벤션

커밋 메시지는 `<type>: <한국어 설명>` 형식을 따릅니다 (기존 커밋 로그 기준).

- `feat`: 새로운 기능/애셋 추가 (예: `feat: 플레이어 이동 및 카메라 추적 기능 추가`)
- `fix`: 버그 수정
- `init`: 프로젝트/모듈 최초 세팅
- `update`: 기존 기능·설정 개선
- `refactor`: 동작 변화 없는 구조 개선
- `chore`: 빌드 설정, .gitignore 등 부수 작업

제목은 한 줄 요약으로 작성합니다. 여러 변경이 섞여 있으면 본문에 항목을 나눠 적습니다.

## 작업 단위

- 스크립트와 그 스크립트를 사용하는 프리팹은 함께 한 커밋으로 묶습니다
  (예: `ChangeColorBarController.cs` + `FadeEffect.cs` + `ChangeColorBar.prefab`).
- 서로 관련 없는 변경(다른 기능의 프리팹/스크립트)은 별도 커밋으로 분리합니다.

## 워크플로우

새 애셋을 추가하거나 수정한 뒤 커밋하려면 `/commit-review`를 사용하세요.
직전 커밋 이후 변경 사항을 요약하고, 위 컨벤션에 맞는 커밋 메시지를 제안한 뒤,
승인을 받으면 그 때 커밋까지 진행합니다. 승인 없이 임의로 커밋하지 않습니다.
